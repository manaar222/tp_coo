[project]
name = "tp-coo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Manar", email = "manar.chikhi@univ-tlse3.fr" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
tp-coo = "tp_coo:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
